# will/did mount


```conf
前者会在组件首次渲染前触发，后者则在组件挂载完成后立即触发。
使用前者似乎是正确的做法，毕竟我们希望尽快加载数据，不过需要注意一点。
`实际上，服务端渲染和客户端渲染都会触发 componentWillMount 函数。`
第 8 章将详细介绍服务端渲染。现在你只需要知道，当在服务端渲染组件时，触发异步 API
会带来预料之外的结果。
因此，我们只能用 componentDidMount 钩子，这样就能确保只在浏览器端调用 API 请求
```
